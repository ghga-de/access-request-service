[project]
name = "ars"
version = "6.0.0"
description = "Access Request Service"
dependencies = [
    "ghga-event-schemas ~= 10.0.0",
    "ghga-service-commons[api,auth] >= 5, <6",
    "hexkit[mongodb,akafka] >= 6, <8",
    "httpx >= 0.28",
    "typer >= 0.16",
]

[project.urls]
Repository = "https://github.com/ghga-de/access-request-service"

[project.scripts]
ars = "ars.__main__:cli"
