[project]
name = "ars"
version = "5.2.0"
description = "Access Request Service"
dependencies = [
    "ghga-event-schemas ~= 9.2.0",
    "ghga-service-commons[api,auth] >= 4.1.2, <5",
    "hexkit[mongodb,akafka] >= 5.4.1, <6",
    "httpx >= 0.28",
    "typer >= 0.16",
]

[project.urls]
Repository = "https://github.com/ghga-de/access-request-service"

[project.scripts]
ars = "ars.__main__:cli"
